## 📝 更新历史 (Changelog)

### [近期更新] - 智能检索与清洗引擎重构
**✨ 新特性 (Features)**
* **多词联合模糊搜索**：搜索框现已支持基于空格分词的多关键字联合检索（例如输入 `50016 火`）。底层自动切分关键字并动态生成 SQL 语句，实现跨字段（内容与规范名称）的精准匹配，告别传统 `LIKE` 查询的死板限制。

**🐛 修复与优化 (Bug Fixes & Optimizations)**
* **彻底重构 INI 配置加载引擎**：
  * 优化 C++ `std::wregex` 与 INI 文件的转义兼容性，自动将双斜杠 `\\` 降级解析，防止正则语法树构建失败。
  * 增加严格的正则异常捕获 (`std::regex_error`) 与日志输出，彻底告别非法字符类（如多余中括号 `[[...]]`）引发的程序闪退。
* **清洗规则 (Clean Rules) 靶向升级**：
  * **页码前缀精准削平**：强化 `StripNumberingRegex`，完美剥离各种变体图纸前缀（如 `P12、`、`Q1、`、`p9、`），无视行首的隐藏控制符。
  * **僵尸数据定点清除**：在黑名单引擎中新增专门针对“建筑面积等概况废话”、“下划线占位符”以及“无意义留白行”的拦截规则，确保入库数据的纯净度。

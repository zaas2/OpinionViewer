# 📝 更新历史 (Changelog)

---

## [v1.2] - 2026-06-28

### ✨ 新特性 (Features)
* **智能增量扫描引擎**：搜索与解析逻辑重大升级，引入“指纹 + 时间戳”双重校验机制。现在**只扫描和提取全新的或被修改过的文件**，已入库且未改动的文件将被极速静默跳过，彻底告别重复解析。
* **双轨沙盒清洗机制**：新提取的数据直接灌入隔离的沙盒模型（`m_importModel`）进行预览清洗，未入库数据与历史库实现物理隔离。
* **在线平滑更新 (OTA Update)**：新增底层静默更新检测机制，启动时自动比对 GitHub 最新 Release 版本，支持无感检测与一键弹窗跳转下载。
* **通用视图组件解耦**：重构 `MyTableView`，移除业务菜单，改由 `aboutToShowMenu` 钩子交由父窗口动态装配。
* **级联撤销栈 (Undo Stack)**：支持右键菜单及 `Ctrl+Z` 撤销删除。动态提取行数据压栈，历史库模式下同步回滚 SQLite 物理数据，沙盒区回滚内存视图。
* **多维快捷键绑定**：增加 `Delete` 键全局批量删除及 `Ctrl+F` 自动感知 Page 状态并精准聚焦搜索框。
* **多线程查重与安全句柄**：解析流程迁移至后台线程池（`QtConcurrent::run`）；修复跨线程调用主数据库句柄的闪退问题，配置后台专用句柄。
* **搜索防抖机制**：引入 300ms 搜索防抖（`QTimer`），避免频繁检索阻塞 UI，并支持根据检索状态动态刷新状态栏提示文案。

### 🐛 修复与优化 (Bug Fixes)
* **修正物理删除失败 Bug**：修复 `deleteOpinionByContent` 中因表名误写导致 SQLite 无法严格匹配、删除行重新搜索后“幽灵复活”的问题。
* **自适应布局控制**：最大化时动态归零内容边距（`ContentsMargins`）并移除圆角，还原时恢复圆角与阴影，消除全屏锯齿。

---

## [v1.1] - 2026-05-20

### ✨ 新特性 (Features)
* **多词联合模糊搜索**：支持空格分词多关键字检索（如 `50016 火`），动态组装 SQL 实现跨字段精准匹配。
* **拖拽路径智能记忆**：支持文件及文件夹拖入直接扫描，并在后台更新 `setting.ini` 的默认路径，保留原有配置文件的排版与注释。

### 🐛 修复与优化 (Bug Fixes)
* **TXT 智能编码嗅探**：引入字节级 UTF-8 合法性校验，支持 ANSI / UTF-8 自动识别混读，防止多行意见被非预期合并。
* **WPS 导出乱码根治**：导出文件头部强制注入 UTF-8 BOM 签名，杜绝源码硬编码中文引发的 GBK 隐式转换。
* **纯 C++ INI 引擎重构**：全面清退双斜杠转义，回归原生单斜杠语法，增加异常捕获杜绝非法正则引发的闪退。
* **清洗规则靶向升级**：剥离变体图纸页码前缀（如 P12、Q1 等），自动过滤建筑面积等概况文本与无意义表单行。